interface SiteProps {
  title: string;
  description: string;
  panelPath: string;
}

export const siteConfig: SiteProps = {
  title: "Node Chain",
  description: "Software Agency",
  panelPath: "/overview",
};
